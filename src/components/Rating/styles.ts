import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    label: {
        color: '#FFB900',
        marginTop: 5,
    },
    stars: {
      display: 'flex',
      flexDirection: 'row',
    },
    icon: {
        width: 35,
        height: 35,
    },
});